name: nix-build

on:
  push:
    branches:
      - main
      - claude
    paths:
      - 'devices/**'
      - 'home/**'
      - 'modules/**'
      - 'flake.nix'
      - 'flake.lock'
  # unfiltered so required checks report on every PR
  pull_request:
  workflow_dispatch:

jobs:
  discover:
    runs-on: nix
    outputs:
      hosts: ${{ steps.list.outputs.hosts }}
    steps:
      - uses: actions/checkout@v6
      - id: list
        run: |
          set -eu
          hosts=$(nix eval --json '.#nixosConfigurations' --apply 'builtins.attrNames')
          echo "hosts=$hosts" >> "$GITHUB_OUTPUT"

  build:
    needs: discover
    runs-on: nix
    strategy:
      fail-fast: false
      matrix:
        host: ${{ fromJSON(needs.discover.outputs.hosts) }}
    steps:
      - uses: actions/checkout@v6
      - run: |
          nix build \
            ".#nixosConfigurations.${{ matrix.host }}.config.system.build.toplevel" \
            --print-build-logs

  verify-lock:
    if: ${{ github.event_name == 'pull_request' }}
    runs-on: nix
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - run: |
          set -eu
          if git diff --quiet "origin/${{ github.base_ref }}...HEAD" -- flake.lock; then
            echo "No flake.lock changes to scan."
            exit 0
          fi
          for host in srv-n1 erebos; do
            out=$(nix build ".#nixosConfigurations.$host.config.system.build.toplevel" --print-out-paths)
            nix run github:tiiuae/sbomnix#vulnxscan -- "$out" || echo "vulnxscan reported findings for $host (informational)"
          done

  verify-images:
    if: ${{ github.event_name == 'pull_request' }}
    runs-on: nix
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - run: |
          set -eu
          refs=$(git diff "origin/${{ github.base_ref }}...HEAD" -- '*.nix' \
            | sed -n 's/^+.*image = "\([^"]*\)".*/\1/p' | sort -u)
          if [ -z "$refs" ]; then
            echo "No container image changes to verify."
            exit 0
          fi
          for ref in $refs; do
            echo "Verifying $ref"
            digestref=$(echo "$ref" | sed 's/:[^@/]*@sha256:/@sha256:/')
            nix run nixpkgs#skopeo -- inspect --no-tags --format 'OK: {{.Digest}}' "docker://$digestref"
          done
